<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <meta name="color-scheme" content="dark light" />
  <title>The Lantern & Leylines — D&D Toolkit</title>

  <style>
    :root {
      --bg: #0f1114;
      --panel: rgba(20, 22, 26, 0.72);
      --panel-2: rgba(26, 28, 33, 0.62);
      --ink: #eae6dc;
      --ink-2: rgba(234, 230, 220, 0.75);
      --muted: rgba(234, 230, 220, 0.55);
      --border: rgba(234, 230, 220, 0.16);
      --shadow: 0 14px 40px rgba(0,0,0,0.45);

      --glow: 0 0 18px rgba(155, 208, 255, 0.22);
      --glow-strong: 0 0 28px rgba(155, 208, 255, 0.35);

      --radius: 18px;
      --radius-sm: 12px;
      --gap: 16px;

      --serif: ui-serif, Georgia, "Times New Roman", Times, serif;
      --sans: ui-sans-serif, system-ui, -apple-system, Segoe UI, Roboto, Helvetica, Arial;
    }

    [data-theme="light"] {
      --bg: #f4efe4;
      --panel: rgba(255, 252, 246, 0.88);
      --panel-2: rgba(255, 252, 246, 0.72);
      --ink: #1b1c1f;
      --ink-2: rgba(27, 28, 31, 0.72);
      --muted: rgba(27, 28, 31, 0.55);
      --border: rgba(27, 28, 31, 0.18);
      --shadow: 0 18px 48px rgba(20, 12, 3, 0.16);
    }

    * { box-sizing: border-box; }
    body {
      margin: 0;
      font-family: var(--serif);
      color: var(--ink);
      background:
        radial-gradient(1000px 600px at 20% 10%, rgba(155,208,255,0.14), transparent 55%),
        radial-gradient(900px 600px at 80% 30%, rgba(255,210,150,0.08), transparent 58%),
        var(--bg);
    }

    .panel {
      background: linear-gradient(180deg, rgba(255,255,255,0.04), transparent 30%), var(--panel);
      border: 1px solid var(--border);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      backdrop-filter: blur(8px);
    }

    /* ================= HEADER ================= */
    header {
      margin: 18px auto 0;
      width: min(1200px, 94%);
    }

    .header-inner {
      padding: 20px 22px;
      display: grid;
      gap: 14px;
      position: relative;
      overflow: hidden;
    }

    .header-inner::after {
      content: "";
      position: absolute;
      inset: -40px;
      background:
        radial-gradient(400px 200px at 20% 30%, rgba(155,208,255,0.18), transparent 60%),
        radial-gradient(400px 200px at 80% 70%, rgba(255,210,150,0.12), transparent 60%);
      animation: pulse 5s ease-in-out infinite;
      pointer-events: none;
    }

    .brand {
      display: grid;
      gap: 6px;
    }

    .brand h1 {
      margin: 0;
      font-size: 30px;
      letter-spacing: 0.3px;
    }

    .tagline {
      margin: 0;
      font-family: var(--sans);
      font-size: 14px;
      color: var(--ink-2);
      max-width: 80ch;
    }

    .header-row {
      display: flex;
      flex-wrap: wrap;
      gap: 12px;
      align-items: center;
      justify-content: space-between;
    }

    .quote {
      font-family: var(--sans);
      font-size: 13px;
      color: var(--ink-2);
      border: 1px dashed rgba(234,230,220,0.25);
      padding: 10px 12px;
      border-radius: var(--radius-sm);
      background: var(--panel-2);
      max-width: 70ch;
    }

    .controls {
      display: flex;
      gap: 10px;
      flex-wrap: wrap;
    }

    .btn {
      border-radius: 999px;
      padding: 9px 14px;
      border: 1px solid var(--border);
      background: rgba(0,0,0,0.18);
      color: var(--ink);
      font-family: var(--sans);
      font-size: 13px;
      cursor: pointer;
    }

    .btn:hover {
      box-shadow: var(--glow);
      border-color: rgba(155,208,255,0.4);
    }

    /* ================= MAIN ================= */
    main {
      width: min(1200px, 94%);
      margin: 22px auto 30px;
    }

    .intro {
      margin-bottom: 18px;
      font-family: var(--sans);
      color: var(--ink-2);
      font-size: 14px;
      max-width: 80ch;
    }

    .grid {
      display: grid;
      grid-template-columns: repeat(auto-fit, minmax(240px, 1fr));
      gap: var(--gap);
    }

    .card {
      padding: 18px;
      border-radius: var(--radius);
      border: 1px solid var(--border);
      background: rgba(0,0,0,0.18);
      transition: transform 180ms ease, box-shadow 180ms ease;
      position: relative;
      overflow: hidden;
    }

    .card:hover {
      transform: translateY(-4px);
      box-shadow: var(--glow-strong);
    }

    .icon {
      font-size: 28px;
      margin-bottom: 6px;
    }

    .card h3 {
      margin: 6px 0 4px;
      font-family: var(--sans);
      font-size: 16px;
    }

    .card p {
      margin: 0;
      font-family: var(--sans);
      font-size: 13px;
      color: var(--ink-2);
    }

    .open {
      margin-top: 10px;
      display: inline-block;
      font-family: var(--sans);
      font-size: 13px;
      padding: 8px 12px;
      border-radius: 12px;
      border: 1px solid var(--border);
    }

    .open:hover {
      box-shadow: var(--glow);
    }

    footer {
      margin-top: 22px;
      font-family: var(--sans);
      font-size: 12px;
      color: var(--muted);
      display: flex;
      justify-content: space-between;
      flex-wrap: wrap;
      gap: 10px;
    }

    @keyframes pulse {
      0%,100% { opacity: 0.6; }
      50% { opacity: 1; }
    }
  </style>
</head>

<body data-theme="dark">

<header class="panel">
  <div class="header-inner">
    <div class="brand">
      <h1>The Lantern & Leylines</h1>
      <p class="tagline">
        A parchment-smooth toolkit for heroes, heralds, and hexes — roll true, plan fast, and let the story breathe.
      </p>
    </div>

    <div class="header-row">
      <div class="quote" id="quote">
        <strong>Arcane Notice:</strong> “Speak the tool’s name, and the table obeys.”
      </div>

      <div class="controls">
        <button class="btn" id="themeToggle">🌗 Dark Mode</button>
        <button class="btn" id="newQuote">✨ New Omen</button>
      </div>
    </div>
  </div>
</header>

<main>
  <p class="intro">
    Choose your instrument: the bones for fate, the ledger for battle order,
    the scroll for heroes, or the generators that conjure a town from rumor and smoke.
  </p>

  <section class="grid">
    <a class="card" href="dice.html">
      <div class="icon">🎲</div>
      <h3>Dice Roller — quick rolls</h3>
      <p>Roll the bones and let fate speak.</p>
      <span class="open">Open →</span>
    </a>

    <a class="card" href="initiative.html">
      <div class="icon">⚔️</div>
      <h3>Initiative — battle order</h3>
      <p>Track turns without losing momentum.</p>
      <span class="open">Open →</span>
    </a>

    <a class="card" href="character-sheet.html">
      <div class="icon">📜</div>
      <h3>Character Sheet — stats & story</h3>
      <p>Your legend, neatly inked.</p>
      <span class="open">Open →</span>
    </a>

    <a class="card" href="town-generator.html">
      <div class="icon">🏘️</div>
      <h3>Town Generator — places & plots</h3>
      <p>From taverns to trouble.</p>
      <span class="open">Open →</span>
    </a>

    <a class="card" href="character-generator.html">
      <div class="icon">🧙</div>
      <h3>Character Generator — heroes on demand</h3>
      <p>Strangers with secrets.</p>
      <span class="open">Open →</span>
    </a>

    <a class="card" href="shop-generator.html">
      <div class="icon">🛒</div>
      <h3>Shop Generator — wares & wonders</h3>
      <p>The prices bite. The loot is real.</p>
      <span class="open">Open →</span>
    </a>
  </section>

  <footer>
    <span><strong>Unofficial fan tool.</strong> Not affiliated with Wizards of the Coast.</span>
    <span>Version 0.1</span>
  </footer>
</main>

<script>
  const quotes = [
    "“Steel sings loudest when courage keeps the rhythm.”",
    "“Never trust a smiling bard… unless the snacks are good.”",
    "“A rune ignored is a lesson repeated.”",
    "“Heroes are made between the roll and the resolve.”",
    "“Speak softly. Cast precisely. Take notes.”"
  ];

  const quoteBox = document.getElementById("quote");
  document.getElementById("newQuote").onclick = () => {
    quoteBox.innerHTML = "<strong>Omen:</strong> " + quotes[Math.floor(Math.random() * quotes.length)];
  };

  const toggle = document.getElementById("themeToggle");
  toggle.onclick = () => {
    const body = document.body;
    const dark = body.getAttribute("data-theme") === "dark";
    body.setAttribute("data-theme", dark ? "light" : "dark");
    toggle.textContent = dark ? "🌗 Light Mode" : "🌗 Dark Mode";
  };
</script>

</body>
</html>
